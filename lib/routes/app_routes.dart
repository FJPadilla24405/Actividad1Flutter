import 'package:flutter/material.dart';


class AppRoutes {
  // Definir nombres para las rutas
  static const String theme = '/';

  // Mapa de rutas
  static final Map<String, WidgetBuilder> routes = {
    //theme: (context) => const a(),
  };
}